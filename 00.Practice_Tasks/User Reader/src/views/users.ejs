<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Users</title>
</head>

<body>
    <% allUsers.forEach(elem=>{ %>
        <a href="./users/<%= elem._id  %>">
            <img src="<%= elem.image %>" alt="">
            <h1>
                <%= elem.name %>
            </h1>
        </a>
        <% }) %>
</body>

</html>